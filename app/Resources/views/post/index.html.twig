{% extends 'base.html.twig' %}

{% block body %}
     {% for post in posts %}
    <div class="blog-post">
            <h2 class="blog-post-title"><a href="{{ path('post_show', { 'id': post.id }) }}">{{ post.title }}</a></h2>
            <p class="blog-post-meta">
                {% if post.created %}
                    {{ post.created|date('Y-m-d H:i:s') }}
                {% endif %}
                {% if post.published %}
                    <span class="glyphicon glyphicon-ok"></span>
                {% else %}
                    <span class="glyphicon glyphicon-remove"></span>
                    
                {% endif %}
                <a href="{{ path('post_edit', { 'id': post.id }) }}">Edit Post</a>
            </p>
            
            <p>{{ post.description }}</p>
            
           
          </div>

 
            {#<tr>
                <td><a href="{{ path('post_show', { 'id': post.id }) }}">{{ post.id }}</a></td>
                <td></td>
                <td></td>
                <td>{{ post.text }}</td>
                <td>{% if post.published %}Yes{% else %}No{% endif %}</td>
                <td></td>
                <td>{% if post.updated %}{{ post.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('post_show', { 'id': post.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('post_edit', { 'id': post.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>#}
        {% endfor %}
        
    <ul>
        <li>
            <a href="{{ path('post_new') }}">Create a new post</a>
        </li>
    </ul>
{% endblock %}
